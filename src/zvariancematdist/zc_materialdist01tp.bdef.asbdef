projection;
strict ( 2 );
define behavior for ZC_materialdist01TP alias materialdist
use etag

{
  use create;
  use update;
  use delete;

  use action createVarianceData;
  use action calculateVariance;
  use action postVariance;

  use association _matdistlines { create; }
}

define behavior for ZC_matdistlinesTP alias matdistlines
use etag

{
  use update;
  use delete;

  use association _materialdist;
}