interface;
define behavior for ZI_materialdist01TP alias materialdist
use etag

{
  use create;
  use update;
  use delete;

  use action createVarianceData;
  use action calculateVariance;
  use action postVariance;

  use association _matdistlines { create; }
}

define behavior for ZI_matdistlinesTP alias matdistlines
use etag

{
  use update;
  use delete;

  use association _materialdist;
}